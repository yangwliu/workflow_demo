name: Test_Workflow
on:
  push:
    branches:
      - '*'
  schedule:
    - corn: '*/10 * * * *'

jobs:
  echo_hello:
    runs-on: ubuntu-latest
    steps:
      - name: printhello
        uses: actions/checkout@v4
